// Direct scribe to the DOM.

export const showId = () => {
  const showId = document.getElementById("id").innerHTML;
};

export const showEmail = () => {
  const showEmail = document.getElementById("email").innerHTML;
};

export const showUsername = () => {
  const showUsername = document.getElementById("username").innerHTML;
};

export const showUserId = () => {
  const showUserId = document.getElementById("user_id").innerHTML;
};